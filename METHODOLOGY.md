# Midas — Methodology & Limitations

*Pre-registered evaluation methodology. This document states what Midas measures, how, and — deliberately, up front — everything that is wrong or unproven about it. It is versioned in git, and every substantive change is logged, dated, in the changelog at the end of this document — the public record of what was claimed when. (The full commit history lives in the live-desk repository, which is not public today; see "The engine is open source" below.)*

Last substantive update: 2026-08-07.

---

## What Midas is

Midas is a public experiment: ten Claude trading personas, each started with €10,000 of **paper** money on **2026-04-17**, evaluate the market and journal their reasoning autonomously every weekday with no human in the loop — authoring an order only when conviction clears the bar, which most agents do on a minority of sessions (see the live cadence figures below). An eleventh agent, the Oracle, narrates but does not trade. Every order, the reasoning behind it, every fill, and every portfolio snapshot is committed to a git repository and rendered on this site. The engine that runs the desk is open source (MIT); the full live-desk repository — the complete data ledger — is not public today. The site at `midas.revah.paris` renders entirely from those committed artifacts.

This is an experiment in *process and transparency*, not a demonstration of trading skill. Read the limitations section before drawing any conclusion from a number on the leaderboard.

## What is measured

For each agent we track the EUR mark-to-market value of its portfolio over time, and its return since inception. Returns convert all positions to EUR (the operator is a French tax resident); a USD-denominated holding therefore carries a currency-translation effect — see *Known distortions* below.

## The controls (this is the part most projects skip)

A return number in isolation is meaningless. Every agent is graded against **two controls**, both rendered publicly on its dossier:

1. **Passive benchmark** — a buy-and-hold of the agent's natural reference (e.g. SPY-equivalent for a US-equity agent, BTC-EUR for the crypto agent, a flat EUR-cash line for the FX agent). Beating this is the minimum bar for "the trading added anything."
2. **Coin-flip phantom** — a portfolio that made *random* trades of the same cadence and sizing in the same universe. Beating this is the minimum bar for "the decisions were better than chance."

A global **MSCI World** reference line appears on the leaderboards as a market-wide anchor.

An agent that beats its benchmark but loses to its own coin-flip has demonstrated nothing but luck. We show both deltas next to every return so that this cannot be hidden.

## Costs

From **2026-06-13**, paper fills carry a realistic, per-asset-class fee model (`engine/fees.py`):

- **Equities / ETFs**: ~0.05% with a €1.25 per-order floor — Interactive Brokers Ireland tiered pricing.
- **Crypto**: 0.40% (Kraken spot taker; the cheaper 0.25% maker rate is *not* assumed — we use the conservative number).
- **FX**: ~0.002% spread proxy.

Fees are deducted from cash on every fill and tighten the cash-availability check. **Historical `bt` backtests (factor research and the standalone backtester service) are gross of costs**, however: `bt`'s commission hook does not expose the ticker, so the per-asset-class fee model cannot be mapped into it faithfully without a non-trivial signature adaptation. That full wiring is deferred; until then those backtests carry an explicit `GROSS_OF_COSTS` warning in the CLI output and the backtester API `warnings` field, and their returns should be read as fee-free. **Before 2026-06-13 the paper simulation modelled zero fees.** We do not retroactively rewrite history; the pre-fix period is fenced and returns from that window are fee-free and should be read as such. A separate **after-tax shadow ledger** (`data/tax_shadow/`) estimates the French flat tax (PFU, 30%) that realized gains would incur — reporting only; it never affects the portfolios. Its crypto figure is a documented per-coin approximation of the statutory global-portfolio method (see the module docstring); it will be replaced with the exact method before any real capital is deployed.

## The noise statement

**Eight weeks is statistical noise.** With roughly 35 daily observations, the variance of returns dwarfs any plausible skill signal; distinguishing skill from luck at conventional confidence would take years of data, not weeks. Accordingly:

> **No claim of skill, edge, or "Claude can trade" will be made on the basis of this experiment until at least 6 months have elapsed AND each agent has accumulated ≥100 fills — and even then, only relative to the two controls above, net of fees and modelled tax.**

This threshold is pre-registered here so that a later good run cannot be retrofitted into a skill claim. The live status against that threshold — fills accumulated per agent, the binding constraint, and whether the desk is on track — is generated below from the same data as the leaderboard, not hand-typed.

## Known distortions in the current leaderboard

These are real and we surface them rather than letting a reader discover them as a "gotcha." Figures below are illustrative, drawn from the standings around 2026-06-06; the live site carries current numbers.

- **Currency translation, not skill.** The EUR weakened against the USD over the sample. Because returns are reported in EUR, every USD-denominated book received a mechanical translation boost. For the leading agent (`sharp-shooter-usd`, ~+7.8% at the time), roughly **2.35 percentage points** of the return is this currency-accounting effect, not a trading decision.
- **Universe mix, not selection.** US equities outperformed European equities by ~6pp over the sample. A further ~3.5pp of that same agent's lead reflects *which market it was assigned*, not which stocks it picked. Net of both effects, the nominal leader and the second-place agent are within ~0.1pp of each other in native-currency terms — a statistical tie.
- **Day-one inception artifact.** One agent (`world`) recorded a first-day mark-to-market above its €10,000 baseline, inflating its reported return by an amount unrelated to subsequent decisions (true return from actual day-1 NAV is materially lower than the headline).
- **Lost fill, `sharp-shooter-eur` (2026-05-21) — reconciled 2026-08-02.** A conditional-order sale was confirmed by the broker but its portfolio write was lost to an infrastructure bug. This was originally disclosed and *not* corrected; on 2026-08-02 that refusal was reversed, because keeping it meant publishing a book that had sold four shares out of three. The lost sale was inserted, the later sale it invalidated was removed, the corresponding inbox fill was voided rather than deleted, and the reported return moved from **+3.30% to +0.19%**. The rewrite itself went unlogged for five days. Full record in the [2026-08-07 changelog entry](#sweep-and-restatement-2026-08-07); the original incident and the reasoning that was overridden are in the [2026-07-27 entry](#lost-fill-2026-05-21).
- **Survivorship bias in historical factor research.** The index universes (`sp500`, `nasdaq100`, `cac40`, `dax`, `ftse100`, `stoxx-600`) resolve to their **current** constituents, committed in `data/universes/*.json`. Backtesting one of these over a historical window silently trades only the names that survived to today — the delisted losers were never in the sample. An early factor-research run against the S&P 500's current membership from 2024 reported returns inflated by roughly **194%** versus the same run on a survivorship-free universe. We cannot reconstruct point-in-time membership without a historical-constituents feed we do not have, so the mitigation is a loud `SURVIVORSHIP_BIAS` warning (`engine/survivorship.py`) emitted by the CLI backtesters and the backtester API whenever a run's start date predates the universe file's last refresh. The factor-research and Add-a-Strategy defaults were moved to `dow30` (a stable, slow-moving 30-name universe) and `etf-broad`, which do not carry this distortion.

## How execution is disciplined

- **Safety lives in the broker, not the prompt.** The paper broker enforces 15 distinct rejection/cancel reason codes (cash, shares, notional, universe, drawdown, FX-rate, order-count, trigger-expiry, agent cancellations, and more). A persona's prompt is aspirational; the broker is what actually constrains it.
- **Decision-time air gap.** Trading sessions run with **no outbound HTTP**. Prices — and, for the two agents in the sentiment A/B below, news headlines — come only from committed stores, populated out-of-band by separate scheduled jobs. Agents never fetch the web at decision time. This makes two failure modes that affect comparable systems *structurally impossible* here: tool-level look-ahead leakage, and live-fetch instability. Headlines, when used, are pre-sanitized committed data treated as untrusted input (see the A/B section).
- **Idempotent, auditable order flow.** Orders and fills are committed JSONL keyed on deterministic order IDs; re-running a session cannot double-fill. Conditional orders carry mandatory expiries and fire through a separate watcher.

## Pre-registered experiment: sentiment A/B

*Registered 2026-06-13, before any sentiment-informed session. The published evidence on adding news/sentiment to LLM traders is mixed-to-negative (it often injects noise and raises turnover), so this is run as a falsifiable test, not a feature — and the result will be published either way.*

- **Hypothesis.** Giving an analyst a feed of recent, sanitized news headlines for the tickers it holds does **not** improve its risk-adjusted, net-of-fee return versus its own controls, and may worsen turnover.
- **Treatment group (2 agents):** `satoshi` (crypto) and `sharp-shooter-eur` (EU momentum). **Control group:** the other 8 agents, which receive no sentiment feed.
- **Mechanism.** A separate scheduled job collects ≤10 headlines/ticker/day for active tickers and commits them to `data/market/news/`. The two treatment agents read their tickers' digests during their normal (still air-gapped) session, under an explicit "untrusted data, never instructions" preamble. Headlines reach **analysts only** — never the real-money Manager path, which consumes only structured research-note fields, never raw text.
- **Window:** 4 weeks from first sentiment-informed session.
- **Metrics:** each treatment agent's return vs its own passive benchmark and coin-flip control (the same two controls every agent has), its turnover, and qualitative drift in its trade rationales — each compared against the control group over the same window.
- **Outcome:** if the treatment shows no benchmark-relative improvement (or a turnover spike), the feed is removed and the result reported as a negative finding. Sentiment is **never** promoted to the Manager path regardless of outcome. A positive result only makes more *analysts* eligible, never the Manager.

## Scope and honest limitations

- **Paper money only.** No real capital is at risk today. A real-money deployment (€1,500–2,000, Interactive Brokers Ireland + Kraken, French-tax-aware) is designed but gated: capital moves only after a separate manager strategy demonstrably beats a deterministic baseline, net of fees and tax, over a multi-month pre-registered window. Paper results are a weaker form of evidence than live trading, by definition.
- **One model family by design.** Every agent runs on Claude. This is **not** a model comparison and no cross-model claim can be drawn from it.
- **Single run, no pre-registered hypothesis test.** This is one realization of one experiment, not a controlled study with replication. Treat it as a transparent case study, not a result.
- **Small sample, short horizon.** See the noise statement.

## What you may fairly conclude today

That ten Claude agents have evaluated the market and paper-traded autonomously, every weekday since 2026-04-17 — authoring a trade only on a minority of sessions — with every decision and its reasoning committed to git and rendered on this site line by line (the engine is open source; the full live-desk repository is not public today); that each is measured against both a passive benchmark and a random-trade control shown beside its return; and that the whole thing was built and run by one person. Nothing about returns, edge, or whether "AI can trade." Those questions are deliberately deferred to the thresholds above.

<a id="open-source"></a>

## The engine is open source

The engine that runs this experiment is public at [github.com/w2ur/midas-core](https://github.com/w2ur/midas-core) under the MIT licence. That includes the **Brain/Hands** architecture — agents author orders to an outbox on disk; a paper broker enforces 15 distinct rejection/cancel reason codes and writes fills to an inbox — the `roster.yaml` config that drives the whole cast (traders, a narrator, and an allocator, each with its own safety rails enforced in the broker rather than the prompt), and a runnable demo desk with a documented walkthrough. Anyone can clone it and stand up their own desk: the quickstart in the [midas-core README](https://github.com/w2ur/midas-core#readme) is the starting point. A fuller account of the architecture, the rails, and what is and is not public is at [/open-source](/open-source).

What is **not** public is this live desk's own repository — the full data ledger of orders, fills, and snapshots. The site renders those committed artifacts, but the repository itself is private today. So the framework is open and reproducible; the specific book you are reading here is shown, not shared.

## Methodology changelog

*Methodology changes are logged here rather than silently applied — pre-registration honesty.*

- <a id="sweep-and-restatement-2026-08-07"></a>**2026-08-07 — The price history swept, the published track record restated, and a ledger rewrite from 2026-08-02 logged here five days late.** This is the largest change to the published record since the experiment began, and it is a deliberate, one-time event. What changes is mostly the price each holding was valued at, and the composition that valuation was taken over. **An earlier draft of this entry said fills, cash and the trade ledger were byte-identical, and that no file under `data/orders/`, no `trades.json` and no `portfolio.json` was modified anywhere in this work. That was true when written and is no longer true.** The third defect described below turned out to have corrupted **23 fills at execution time**, not merely their later valuation, so the ledger *is* rewritten for those 23 — and only those. The corrected sentence: every other fill, and every one of the other 203 trades, is byte-identical; the 23 exceptions are enumerated, keep their original values inline under `originally_recorded`, and are accounted for below. The check that verifies this is the same one, still confirmed capable of firing by running it against the 2026-08-02 commit described next, where it matches.
  **First, the omission — this changelog missed a ledger rewrite.** On 2026-08-02, commit `04bb2371b` reconciled the `sharp-shooter-eur` lost fill recorded in the [2026-07-27 entry](#lost-fill-2026-05-21) below. It inserted the confirmed-but-lost `ord_2026-05-21_sharp-shooter-eur_001` (SELL 1 ASML.AS @ €1,249) into that book's trade log and removed `ord_2026-06-24_sharp-shooter-eur_001` (SELL 1 ASML.AS @ €1,560.80), the later sale that only three shares ever bought could not support; it restated 41 of that book's 66 snapshots, from 2026-05-22 to 2026-07-31; and it voided rather than deleted the fill row in `data/orders/inbox/2026-06-25.jsonl`, rewriting its status to `rejected`/`NO_POSITION_TO_SELL` while keeping the original fill under an `originally_recorded` key and a `reconciliation_note`, so the record still shows what the broker actually did. The book's cash went from €10,329.90 to €10,019.35 and its **published return from +3.30% to +0.19%**. That reversal of the 2026-07-27 refusal was correct on the merits — a ledger showing four shares sold out of three is not a track record either — and the €310.55 it removed is, to the cent, the same gap the 2026-07-27 entry had already published as its counterfactual (€10,338.40 − €10,027.85). **But no changelog entry was written for it, and the 2026-07-27 entry was left asserting "The ledger is not being rewritten" for five days after it had been.** That sentence is corrected in place below. This is the failure mode this document exists to prevent, and it was caught by a cross-check on a different task, not by any process. `sharp-shooter-eur`'s return today, after the restatement described below, is **−0.18%**.
  **What was wrong with the prices.** Bars were written to the committed OHLCV store while still forming and then frozen, throughout history — the defect the [2026-08-06 entry](#stale-prices-2026-08-06) below disclosed but repaired for one day only. Separately, eleven tickers underwent a split-type corporate action *during* the experiment, and their entire pre-action history stayed on the old basis while new bars arrived on the new one, leaving a genuine discontinuity mid-series. Read raw, the pre-sweep store showed `WLN.PA` gaining **+3,766.94%** in a day on 2026-06-11, `TIT.MI` **+895.83%** the same day, `KLAC` losing **−89.45%** on 2026-06-12, `CVNA` **−79.45%** on 2026-05-07 and `CRWD` **−74.90%** on 2026-07-02. None of this was fabricated data; it was correct data on two incompatible bases stitched together.
  **The sweep.** Commit `4b6b8556` re-fetched and rewrote **463 price files** (plus the ticker-name registry), **revising 29,348 rows**. Of those, **20,448 changed the daily close** — the real corrections. **6,915 changed only `adj_close`**, which is not an error at all but Yahoo Finance re-basing its dividend-adjusted series across history after each payout; the remaining **1,985** changed only open/high/low/volume. (The sweep's own commit message reports 22,433 close corrections; that figure counts the 1,985 non-close revisions as close corrections. The correct number is 20,448.) **1,056 rows were added**, all forward catch-up: no file's history was deepened past its own oldest pre-existing row, no row was deleted, and the relative order of every pre-existing row is unchanged — each symbol was windowed to its own history precisely so that "correct the bars we have" could not quietly become "fetch more bars."
  **The eleven corporate actions,** with Yahoo's own ex-dates and ratios, re-verified against the vendor rather than taken from the sweep: `CVNA` 5:1 (2026-05-08), `FCIT.L` 4:1 (2026-05-11), `FDX` 1.241 (2026-06-01), `AI.PA` 1.1 (2026-06-08), `KLAC` 10:1 (2026-06-12), `TIT.MI` 1-for-10 (2026-06-15), `WLN.PA` 1-for-40 (2026-06-15), `DD` 1-for-3 (2026-06-24), `HON` 0.9535 (2026-06-29), `SPGI` 1.057 (2026-07-01), `CRWD` 4:1 (2026-07-02). **18,027 rows across those eleven files were revised.** **No agent ever held a position through one of them, and that was verified, not assumed:** of the eleven, exactly one — `AI.PA` — was ever traded by any of the twelve books, bought by `steady-eddie-eur` on 2026-08-04, two months *after* its 2026-06-08 ex-date, at a post-action price. (The check was confirmed capable of firing by running it against a synthetic position opened before and closed after an ex-date.) Nor did any published control series carry the artifact: the passive benchmarks are single tickers, none of them on this list, and although seven of the ten coin-flip universes contain at least one of these names, the largest single-day move in any of the twenty published control series before the sweep is **−7.31%** (`satoshi`'s coin flip, an ordinary day in crypto) and the largest in any non-crypto control is **+4.61%** — nothing resembling a split discontinuity. So the damage was latent in the store rather than realised in a published number. It was still worth correcting, because **the engine had no corporate-action handling of any kind**: a position held through a split would have kept its pre-split share count against a post-split price, two errors compounding in the same direction. `engine/corporate_actions.py` now detects a split from the store's own disagreement with a fresh fetch and share-adjusts held positions, and a weekly scheduled resweep of held tickers exists to give it something to detect — the nightly fetch's one-day overlap structurally cannot. The sweep also fixed one unrelated bad tick, `FCIT.L` on 2026-05-08, stored at 5,275.02 against a 320–330 range.
  **A second defect, found while building the restatement: cross-currency positions were never converted when valued.** The fill path had always converted a foreign-currency notional into the book's base currency; **two of the three valuation paths had not** — `scripts/daily_session._compute_positions_value`, which writes the snapshots, and `engine.valuation.portfolio_mtm`, which feeds the public leaderboard and the drawdown rail. Both summed a foreign close straight into a EUR book as though it were already EUR. (The third path, the restatement valuer written for this work, converted from the start.) Measured at 2026-08-06, the leaderboard's own valuation date, five books were affected: `goldfinger` (`PHAG.L`, GBP) by **+€280.28**, or **+2.80 percentage points** of its reported return; `world` (`SIKA.SW` CHF, `LLOY.L` GBP) **+€238.12 / +2.38 pp**; `monsieur-forex` (`AUDUSD=X`, USD) **−€150.83 / −1.51 pp**; and the two private manager books, `the-manager` **+€8.65 / +0.43 pp** and `baseline-manager` **−€41.49 / −2.07 pp** of their €2,000 each. Net **+€334.74**. Both paths now convert through the same two helpers the broker uses, and a book whose FX rate is unavailable is skipped loudly rather than silently under-valued.
  **A third defect, found after that restatement had been applied: a ticker's currency was guessed from its suffix.** Those "same two helpers" resolved a ticker's currency by string suffix — every `.L` to GBP, every currency pair and every unenumerated suffix to USD. That guess was wrong on **7 of the 63 tickers this desk has ever traded**, in three distinct ways, each on a real position. **(1) The London Stock Exchange quotes in pence, not pounds.** `world` holds 8 `LLOY.L` at 117.03p; the book carried them at **€1,092.55** instead of **€10.93** — a factor of 100. **(2) `.L` is not uniformly sterling.** `PHAG.L` (WisdomTree Physical Silver) and `CRUD.L` quote in dollars, so `goldfinger`'s and `the-manager`'s metal and oil were valued as pounds: on 2026-05-13 alone, `goldfinger`'s 25 `PHAG.L` were booked at **€2,321.12** rather than **€1,714.36**, and its 105 `CRUD.L` at **€1,924.06** rather than **€1,421.09**. **(3) A currency pair is quoted in its second leg, not in dollars.** `EURJPY=X` quotes in yen, `USDCAD=X` in Canadian dollars, `USDCHF=X` in francs, `EURGBP=X` in sterling — `monsieur-forex` had all four valued as US dollars. Its 8 units of `EURJPY=X` on 2026-07-07 were carried at **€1,296.67** instead of **€8.00**. A fourth failure mode was latent rather than realised: every European suffix the heuristic did not enumerate fell through to USD, and of the 132 previously-USD European tickers in `world`'s own 1,077-name universe only 70 are eurozone — the rest quote in SEK (32), NOK (12), DKK (10) and PLN (2). None is currently held, but snapshots are immutable, so the first such purchase would have frozen wrong.
  Currency resolution now lives in `engine/quotes.py` and asks three sources in order: the hand-maintained `data/ticker_currencies.json` override map, then **the vendor's own answer**, captured for 1,019 of 1,190 tickers into `data/tickers.json` by the fetch job that was already reading it, then the suffix heuristic as a last resort. `GBp` is treated as a *unit* rather than a currency, and `normalise_quote` is the single place the pence-to-pounds division is applied. The site's display path (`site/src/lib/ohlcv.ts`) mirrors the same three layers, so the dossier tables and the ledger finally agree on how a position is denominated.
  **Re-valuing was not enough, and this is the one place on this branch where the ledger itself is rewritten.** Re-pricing the snapshots against the corrected resolver moved 92 rows and dropped `world` to **−7.47%** — but that number was itself an artifact, and chasing it down is what exposed the real depth of the defect. **The broker did not merely mis-*value* these positions; it mis-*paid* for them.** The same guessed currency was used to convert each trade's notional into the book's base currency at fill time, so the cash that actually left the book was wrong. `world`'s 8 Lloyds were bought for **€1,090.19** when they cost **€10.90**; that €1,079 never bought anything, and a −7.47% return consisting mostly of it is not a trading result. **23 fills across 4 books** were converted at a guessed currency. They are reconciled by `scripts/reconcile_quote_currency.py`, and cash is restored: `world` **+€1,079.29**, `goldfinger` **+€676.35**, `the-manager` **+€83.11**, `monsieur-forex` **+€21.98**.
  **The correction is multiplicative, never a recomputation, and that distinction is load-bearing.** Re-deriving each notional from `shares × price × rate` fails to reproduce **25 of the 203 unaffected trades** as well — by a median of 0.14% and as much as 13.4% — because the rate a fill executed at is not exactly the daily rate the store serves for that date today, and because the FX series was itself swept earlier on this branch. Recomputing would therefore have folded a silent FX-rate restatement of every touched row into what must be a currency-only correction, and left the corrected rows anchored to a different rate source than the untouched ones. So each affected notional is **scaled** by `(native_price / recorded_price) × (rate_new / rate_old)`, both rates taken from the same source on the same date: the broker's own executed rate stays the anchor, and only the currency identification changes. For `LLOY.L`, where both readings are sterling and only the unit was wrong, the factor collapses to exactly 1/100 — the right answer by inspection. Fees were recomputed only where a fee was actually charged; a fill recorded with `fees: 0.0` predates `engine/fees.py` and does not acquire a retroactive commission. Every mutated record — 23 trades, 23 inbox fills, and one position's cost basis — keeps its prior values under `originally_recorded` with a `reconciliation_note`, the same convention as the 2026-08-02 reconciliation: the record still shows what the broker actually did.
  **Where that leaves the numbers.** Across both passes the restatement changed **110 rows**, again leaving the same **176 unresolved rows** untouched, and changed 83 leaderboard rows across the 87 archive bundles. On the live leaderboard this **supersedes the figures in the paragraph below**: `world` ends at **+3.32%** (against +3.26% before any of this work), `goldfinger` at **−10.91%** (against −12.61%), `monsieur-forex` at **−0.24%**. The other seven are unchanged. **Two claims here were verified rather than asserted.** First, that currency resolution is the sole cause: re-running the identical valuation restatement with only the old suffix resolver restored reproduces every changed row to the cent and leaves all 480 unchanged rows unchanged. Second, that the reconciled ledger is coherent: `initial_capital + full trade replay == recorded cash` now holds to **six decimal places on all twelve books**, and all **223** filled inbox rows agree with their portfolio trade totals to the cent — both checks confirmed capable of firing before being trusted. Inception capital is unmoved by the reconciliation (no affected fill predates any book's first snapshot), so it remains €10,000 / $11,784.11 / €2,000 as described below.
  **What this reconciliation does *not* do, deliberately.** It does not re-litigate the safety rails. Those rails evaluated the wrong notionals and the wrong cash balances at the time, so in principle an order could have been refused on bad arithmetic — but replaying that counterfactual would mean inventing a session that never happened. Instead it was checked directly: of the 57 non-filled records in the four reconciled books, 55 are agent cancellations and 2 are trigger expiries, leaving exactly **one** rail-driven rejection — `ord_2026-08-05_the-manager_001`, `INSUFFICIENT_CASH` — and **it stands under the corrected ledger too**: the order needed €276.16 at its €286.50 trigger against a corrected balance of €260.71. No order in this experiment was refused because of the currency defect. Separately, the **176 unresolved rows keep cash figures derived from the un-reconciled ledger**, since their composition was never reconstructible in the first place; they are the same 176 rows listed below, and they remain the honest gap rather than a patched one.
  **The restatement.** Every snapshot was re-priced against the corrected store. Holdings are not guessed: each row's trade ledger is replayed to the row's *effective session date* — the date whose replayed cash matches the cash the row actually recorded — and then priced at the row's *market* date. Of **748 rows across 12 books**, **455 were re-valued**, **117 changed only their recomputed benchmark reference dict**, and **176 were left exactly as published**. Every row's `date`, `session_date` and `cash` is unchanged, on all 748. **This changed composition as well as price, and that is the finding, not a side effect.** The published series was internally inconsistent: of the 572 rows whose clock could be reconstructed, only **307 can be reproduced at all from the price store as it stood** — and under the market-date reading the published series implied, only 281 — so **265 published valuations could not be reproduced even with the correct holdings and the pre-sweep prices.** The median gap is **0.601%**; 33 rows exceed 2%. The clearest case is `yolo-sapiens-usd` on 2026-04-27, ten days into the experiment: the published row valued a 17-`NVDA`/7-`TSLA` book against cash that already contained the **2026-04-28** sale proceeds — 3 NVDA at $216.61 and 7 TSLA at $378.67, **$3,300.52** counted twice — and reported **+30.44%**. It now reads **+2.43%**. (Its book, like the other two USD twins, was funded at **$11,784.11**, the EUR-equivalent of €10,000 at its own initialization, never a flat $10,000; `roster.yaml`'s 10,000 is a EUR-equivalent target. Each book's inception cash is back-solved from its own ledger: €10,000 for the seven EUR traders, $11,784.11 for the three USD twins, €2,000 for both manager books, and initial capital plus a full trade replay equals recorded cash to the cent on all twelve.) On the published leaderboard the combined effect of the sweep, the FX fix and the restatement moves `goldfinger` from **−17.13% to −12.61%** (+4.52 pp), `world` from **+0.57% to +3.26%** (+2.69 pp), `monsieur-forex` from **+1.06% to −0.46%** and `sharp-shooter-eur` from **+0.86% to −0.18%** — both crossing from profit to loss — while the top two positions are unchanged and four of the ten agents change rank. *(The `goldfinger` and `world` figures here are this pass's, and were superseded the same day by the currency-resolution work described above, which is where the numbers that stand today are given: **−10.91%** and **+3.32%**. They are left as written because this paragraph records what this pass did, not what the final total is.)* The 87 per-day archive bundles were re-derived from the same corrected figures: 57 files and 478 of their 870 leaderboard entries changed, and **no bundle's commentary, trades or posts were altered** (confirmed with a deliberately tampered copy, which the check caught).
  **What was deliberately left as published.** The **176 rows** whose effective session date cannot be reconstructed from the ledger stay byte-identical. Inventing a composition for them would be worse than leaving them alone, so they were not touched: `steady-eddie-usd` 40, `sharp-shooter-usd` 30, `world` 27, `yolo-sapiens-eur` 27, `sharp-shooter-eur` 14 (all inside the window the 2026-08-02 rewrite above restated), `goldfinger` 10, `the-manager` 9, `satoshi` 8, `steady-eddie-eur` 5, `monsieur-forex` 3, `yolo-sapiens-usd` 3. **Three books — `sharp-shooter-eur`, `the-manager` and `yolo-sapiens-eur` — have their *terminal* 2026-08-04 point among them**, so the last figure on those three dossier charts is still priced off the old store while the live leaderboard shows the corrected one. That inconsistency is left standing rather than papered over. Separately, **61 symbols carrying 9,706 drifted rows** (10,097 at any tolerance, 8,793 beyond 1%; measured against the vendor today at a 0.3% threshold) are knowingly **not** repaired: every one of those rows falls at or before **2026-04-17**, the experiment's day one — the oldest is 2016-04-28 and the worst is 47.6% (`SGLN.L`, 2016-06-23) — and no published curve, agent or control, reads a price from before day one. The mechanism behind that drift is not understood; it is not a split, the ratios are scattered rather than constant. It is disclosed here rather than swept, because sweeping ~10,000 rows nobody reads to fix a cause nobody has diagnosed is not a repair.
  **The asymmetry the 2026-08-06 entry disclosed but did not fix is now closed.** Baseline series were rebuilt from day one on every session while snapshots refused replacement by a later session, so a revised close moved a benchmark curve retroactively under a frozen agent curve. Baselines are now **append-or-refuse** on the same contract as snapshots, with a deliberate restatement flag as the only escape hatch. It closed one row too late for some points: **20 of the 176 untouched rows sit on a passive-benchmark line that moved underneath them** (worst 2.71%, `satoshi` 2026-05-22 — `goldfinger` 10, `satoshi` 8, `yolo-sapiens-eur` 1, `sharp-shooter-eur` 1), and **167 of the 176 sit on a coin-flip line that moved** (worst 7.72%, `yolo-sapiens-usd` 2026-07-25; 34 of them beyond 1%), because a coin-flip control is drawn from the agent's whole universe and therefore feels every corrected price. Those points are left as they are.
  **Two corrections to the [2026-08-06 entry](#stale-prices-2026-08-06) below.** First, it stated that no crypto bar exists for **2026-08-05** and that the row was "permanently absent." **That is no longer true, and it is good news:** re-requested over a full-history window rather than a short one, Yahoo Finance does serve that day, and the row is now stored for **32 of the 34** crypto pairs — the same two exceptions as before, `MATIC-USD` and `UNI-USD`, for which the vendor has served nothing since 2025-03-24 and 2025-04-17. It was **recovered from the vendor, not reconstructed** — `BTC-EUR`'s stored close of €55,894.15 matches what Yahoo serves today to the digit. No interpolated or synthetic price has been written into the store, then or now. Second, that entry's closing paragraph describes the benchmark/snapshot asymmetry as disclosed-not-fixed; it is fixed, as above.
  **One new asymmetry, created by this work and disclosed rather than reversed.** Every price reader in the engine takes `adj_close` when a row has one and falls back to `close` — the agent curve and both control curves read the same field, so, contrary to a concern raised while scoping this work, they are not on different bases. But in practice that preference had been inert: before the sweep, `adj_close` equalled `close` on all but **305 of the 78,880** stored rows inside the experiment window (0.39%), because each bar was captured on its own day, before any later dividend had re-based it. The sweep pulled Yahoo's *current* adjusted series, so **8,295 of 79,936 in-window rows (10.4%), across 291 symbols, now carry a dividend-reinvested `adj_close`** — including 16 of the 63 tickers the agents have actually traded, worst `SAN.PA` at **5.30% below** its raw close on 2026-05-04 and `ALV.DE` **4.40% below** on 2026-04-30. **None of the five benchmark tickers is affected.** Concretely: of the 522 restated rows that can be priced both ways, **104 (19.9%) now differ depending on which field is read**, worst 1.71% of portfolio value (`steady-eddie-eur`, 2026-04-20); before the sweep the same measure was 8 rows and 0.34%. So roughly a fifth of restated agent valuations are now on a total-return basis on dates where their benchmark is not. **The direction is not neutral, and should be stated:** a dividend-adjusted historical price sits *below* the raw close, so the affected agent curves are pulled slightly **down on historical dates** — which, against an unchanged present value, mechanically flatters the *slope* from those dates forward. Current valuations are unaffected, because a bar captured today has no later dividend applied to it and its two fields are equal. This is left in place rather than papered over by stripping a field the vendor supplies, but it is a wrinkle, not a design decision anyone made deliberately.
- <a id="stale-prices-2026-08-06"></a>**2026-08-06 — Stale price capture disclosed and fixed; 32 crypto closes corrected, the wider history still frozen, one day unrecoverable.** The OHLCV cron runs at 22:30 UTC, after the US close, but its skip condition was "already have yesterday" rather than "already have today" — so every other run fetched nothing and roughly half of all daily sessions authored trades against an equity close **two market days old** rather than one. The condition is corrected to skip only a store that already holds the current day. Two consequences for 24/7 crypto, where the fetched bar is still forming: **(a)** the store's write path kept only unseen dates and could never revise, so each crypto bar was frozen at its partial value — measured error on 2026-08-04 ranged from −0.40% (`ADA-EUR`) to +1.00% (`DOT-EUR`), with `BTC-EUR` stored at €55,649.74 against a true close of €55,545.09. Of the 34 crypto files in the universe, **32 held a frozen 2026-08-04 row and have been corrected** to their final values; `MATIC-USD` and `UNI-USD` are the exceptions — neither carries a 2026-08-04 row at all, because Yahoo Finance has served no data for either since 2025-03-24 and 2025-04-17 respectively, so there was nothing frozen to correct. The write path now accepts a revision for the trailing day, so this cannot recur. The same repair commit also appended a still-forming **2026-08-06** bar to each of the 32 files — expected, not an error: the next cron run will revise it the same way this one revised 08-04. **(b)** the skip meant no crypto bar was requested at all on 2026-08-05, and Yahoo Finance appeared not to serve that day for any crypto pair. ~~**That row is permanently absent and has not been reconstructed**~~ — **corrected 2026-08-07: the row was recovered from the vendor.** Re-requested over a full-history window rather than a short one, Yahoo does serve 2026-08-05, and it is now stored for every crypto pair; `BTC-EUR`'s €55,894.15 matches the vendor to the digit. It was fetched, not reconstructed — no interpolated or synthetic price has been written into the store, then or now. See the [2026-08-07 entry](#sweep-and-restatement-2026-08-07). Valuations and fills that referenced the wrong 2026-08-04 closes are **not** being restated: the corrections are to the price store going forward, and the ledger stands as executed, consistent with the 2026-07-27 entry below. **Which instruments were affected, corrected:** an earlier draft of this entry said equity and FX bars were verified final at fetch time and were unaffected. That was wrong — it generalised from a three-day sample of one equity and the two mildest currency pairs. Re-measured across instrument classes over each store's 23 most recent bars, against the values Yahoo Finance serves today: **cash equities and ETFs are** final — `SPY` and `AAPL` differ on **0 of 23** days. **Commodity futures are not** — `PL=F`, `GC=F` and `CL=F` each differ on **13 of the 22 days** the two records share, the stored close as much as **3.365%** (`PL=F`, 2026-07-15), **2.865%** (`GC=F`, 2026-07-29) and **2.516%** (`CL=F`, 2026-07-31) away from the true one, because the next Globex session has already opened by 22:30 UTC. **FX is mildly affected** — the rate rolls at 17:00 ET, so `GBPJPY=X` differs on 5 of 23 days, worst **−1.560%** on a Friday; `EURUSD=X` on 5 of 23 but never beyond ±0.16%. Futures and FX bars were being frozen as partial bars exactly as crypto was, and fixing the skip condition alone would have made futures *worse* — the old skip accidentally captured a final previous-day bar on alternating runs, whereas every run now writes a same-day partial. The revision window is therefore applied to **every instrument**, not only crypto. **The historical store remains partially frozen.** Only 2026-08-04 was repaired. Roughly half of all crypto history is still stored at partial values — `DOT-EUR` on **14 of its last 30 bars**, the stored close up to **2.9%** away from the true one, and `ETH-EUR` likewise on 14 of 30, up to **2.1%** — and the futures history is in the same state. The −0.40%…+1.00% range quoted above is scoped to 2026-08-04 alone and is not representative of the store as a whole. A full historical sweep would be a rewrite of the published record on a different scale from a single-day correction, and **it has not been undertaken.** **One asymmetry this creates, disclosed rather than fixed** *(fixed 2026-08-07 — baselines are now append-or-refuse; see that entry)*: the passive-benchmark and coin-flip series are rebuilt from day one on every session, while a portfolio snapshot may not be replaced by a later session (see the 2026-08-03 entry). A revised close therefore moves a benchmark curve retroactively while the agent curve stays exactly as published. Concretely, `satoshi` is benchmarked on `BTC-EUR`: its stored benchmark value for 2026-08-04 is €8,695.39, the next session recomputes it to ≈€8,679.04, and `satoshi`'s own snapshot for that date stays at €9,127.61. With revision enabled this recurs whenever a trailing bar is corrected, not only for this repair. Nothing has been changed to suppress it — a "vs benchmark" delta can move by a fraction of a percent without the equity curve moving.
- <a id="snapshot-overwrite-2026-08-03"></a>**2026-08-03 — Snapshot overwrite disclosed and reverted; the equity curve is now immutable by construction.** Portfolio snapshots are keyed on the *market* date their valuation was priced at, because the passive-benchmark and coin-flip series are dated from the price series and the two must share an axis. The write was an upsert on that key. When a session ran before its own market data landed — the OHLCV cron populates the store at 22:30 UTC, after the 20:00 UTC session — it re-used the previous close's date and **overwrote an already-published row**. On 2026-08-03 the weekday session rewrote the 2026-08-02 weekend-refresh rows for `steady-eddie-usd`, `sharp-shooter-usd`, and `world`, so a published 2026-08-02 portfolio value silently absorbed trades made on 2026-08-03 (`steady-eddie-usd` moved from €13,272.29 to €13,271.04, cash from €991.10 to €1,454.57). The same mechanism had been dropping and restamping rows since the weekend cadence began. **The three rows have been restored** to the values published on 2026-08-02, verified byte-identical against the pre-session commit `18845d8bb` — this reverts an edit the ledger never authorised, the opposite of the reconciliation refused in the entry below. Snapshots now additionally carry `session_date`, and a row may only be replaced by the same session that wrote it; a later session is refused and warns. A session whose price store has not advanced therefore records no new point rather than restating an old one, and the valuation lands on the next real close — the date it was priced at anyway.
- <a id="lost-fill-2026-05-21"></a>**2026-07-27 — Lost fill disclosed: `sharp-shooter-eur` carries a ledger artifact, deliberately not rewritten.** *(The refusal recorded in this entry was reversed on 2026-08-02 and logged on 2026-08-07 — see the [2026-08-07 entry](#sweep-and-restatement-2026-08-07). The entry is kept intact, with the superseded sentence struck through in place, because deleting it would hide the reversal it exists to record.)* On 2026-05-21T21:47:43Z a conditional order (`ord_2026-05-21_sharp-shooter-eur_001`, SELL 1 ASML.AS @ €1249) fired and was confirmed by the paper broker (`status:"filled"` in `data/orders/inbox/2026-05-21.jsonl`), but the matching portfolio write never reached the repository. Cause: `scripts/check_triggers.py`'s `commit_and_push()` omitted `data/portfolios` from its `git add` pathspec, so `apply_trade()`'s mutation landed on the GitHub Actions runner's ephemeral disk and was discarded at teardown, while the inbox confirmation was pushed. The bug was live from commit `418e763bf` (2026-05-18, the watcher's introduction) to `8ff48861e` (2026-05-23, fix); exactly one conditional order fired inside that window. Reported return for `sharp-shooter-eur` today is **+3.38%** (cash €10,338.40). A coherent replay — inserting the lost sale *and* voiding the 2026-06-24 SELL 1 ASML.AS @ €1560.80 that a correctly-stated ledger would have rejected `NO_POSITION_TO_SELL` (the position it sold no longer existed) — lands at **+0.28%** (cash €10,027.85). Inserting the lost sale alone is impossible: it would mean 4 shares sold against 3 bought. ~~**The ledger is not being rewritten.** Every published snapshot, trade, and leaderboard figure since 2026-05-21 stays exactly as executed and committed~~ — **superseded 2026-08-02, logged 2026-08-07: it was rewritten.** Commit `04bb2371b` inserted the lost sale, removed the 2026-06-24 sale it invalidated, voided rather than deleted the corresponding inbox fill row, restated 41 of this book's 66 snapshots, and moved the published return from +3.30% to +0.19%. The reversal went unlogged for five days; the full account is in the [2026-08-07 entry](#sweep-and-restatement-2026-08-07) at the top of this changelog. The reasoning as written on 2026-07-27 stands recorded here rather than being edited away: reconciling it would delete a trade the broker genuinely confirmed and restate 38 of the 63 snapshots published since. That is the trade-off this desk exists to refuse: a track record that can be quietly edited is not a track record. It was overridden because the alternative was a ledger showing four shares sold out of three — an incoherent record is not a track record either — and the correction is logged rather than silent. The gap is now structurally guarded: `tests/test_ledger_integrity.py` diffs every filled inbox row against every portfolio's trade log on every push to `main` (`.github/workflows/session-integrity.yml`) and fails on any *new* divergence — this incident is the sole, named, greppable exception. Root cause fixed 2026-05-23, hardened 2026-06-12 with per-fire commits (removing the batch commit window entirely). The dossier at [`/arena/sharp-shooter-eur`](/arena/sharp-shooter-eur) carries a standing marker on the return figure linking back here.
- **2026-07-24 — Engine open-sourced + repository-visibility wording corrected.** The reusable engine and framework behind this experiment has been public at [`midas-core`](https://github.com/w2ur/midas-core) (MIT) since 2026-07-15 — the Brain/Hands broker, the `roster.yaml`-driven cast, and a runnable demo desk. This document previously described artifacts as "committed to a public git repository"; that phrasing is corrected to distinguish the **open-source engine** from the **private live-desk ledger**. The site still renders every committed artifact; the live repository itself is not public today. Documentation and positioning only — no data or execution behavior changed.
- **2026-07-24 — Manager made public.** The Manager's dossier — portfolio history, fills, and decision log — is now published at [`/arena/the-manager`](/arena/the-manager). This supersedes the "Manager artifacts remain private and off the public site" clause of the 2026-06-28 entry. The Manager stays **off the ranked leaderboard** — different capital, inception, and mandate, so it is not comparable to the ten traders — and its order channels stay isolated from the public inbox. Rationale: the experiment's transparency principle extends to the allocator now that the allocator architecture itself is open in the core.
- **2026-07-03 — Trigger-rails documentation corrected.** Docs previously said conditional-trigger fires apply "the same safety rails" as market orders. In fact the watcher path deliberately skips two *batch-level* rails — `MAX_ORDERS_PER_DAY` and `DAILY_DRAWDOWN_HALT` — while keeping every order-level rail (notional, universe, cash/position/shares, FX, apply_trade). Documentation-only fix (CLAUDE.md, README); no rail behavior changed.
- **2026-07-03 — Backtest honesty + reproducibility.** `bt` backtests are flagged gross of costs (`GROSS_OF_COSTS` warning in the CLI and API; full fee-model wiring deferred because bt's commission hook lacks the ticker). The `random` selector is now seeded deterministically from (strategy id, window start) so factor-research runs reproduce run-to-run instead of reading numpy's global RNG. The CLI `--to` default changed from a hardcoded date to today. Factor-research output is stamped with `generated_at`, the git SHA, and the run arguments.
- **2026-07-03 — Survivorship-bias guard.** Index universes resolve to current constituents, so historical factor-research runs were survivorship-biased (an S&P 500 run from 2024 was inflated ~194%). Added a `SURVIVORSHIP_BIAS` warning (`engine/survivorship.py`) surfaced by `scripts/run_backtest.py`, `scripts/run_all_combos.py`, and the backtester API `warnings` field whenever a run starts before the universe file's last refresh. Moved the factor-research and Add-a-Strategy defaults from `sp500` to `dow30`/`etf-broad`. Reporting-only: no historical result is rewritten.
- **2026-06-28 — Manager recalibrated.** Conviction gate lowered 7→6; acts on a single coherent high-conviction thesis rather than requiring multi-analyst consensus; conditional (trigger) orders enabled for the Manager channel. Rationale: the prior configuration produced an all-cash book across 6 review sessions (conviction 3–6, never reaching the gate), which cannot yield an evaluable Gate C track record. These changes restore the Manager's ability to act on confirmation without front-running, while preserving discipline (HOLD remains default; gate still enforced in code). Manager artifacts remain private and off the public site.
- **2026-06-13 — Fee model introduced.** Paper fills now carry a realistic per-asset-class fee model (`engine/fees.py`). Pre-fix period returns are fee-free and fenced as such. After-tax shadow ledger (`data/tax_shadow/`) added for PFU 30% drag reporting.

## Colophon

- **Cadence.** Weekdays at 20:00 UTC the full ten-agent roster trades; the Oracle narrates. Weekends run a valuation-only refresh — no agents, no new trades — so the leaderboard stays current without manufacturing weekend activity.
- **Execution.** A paper broker enforces 15 distinct rejection/cancel reason codes on every order, and the conditional-order watcher adds a sixteenth (`TRIGGER_EXPIRED`). Safety lives in the broker, not the prompt.
- **Stack.** A Python engine (`bt` for deterministic strategies, `yfinance` for historical data, Claude agents for the analytical ones); the paper broker; and this site — Astro, static output, rendered entirely from committed artifacts and deployed on Vercel.
- **Author.** Built and run by William — [william.revah.paris](https://william.revah.paris).
