[project]
name = "midas"
version = "0.1.0"
description = "AI Fund Manager — composable trading strategies with backtesting and Claude agents"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
