name: auto-merge-session

# Backstop for daily-session commits that landed on a sandbox branch
# instead of main. The trigger pipeline tries `git push origin HEAD:main`
# first via step_git_commit_push; if the harness 403s that (2026-05-08
# incident), the helper falls back to pushing the sandbox branch and
# this workflow takes the merge the rest of the way.
#
# Scope: only commits matching the daily-session message pattern AND
# passing the same artifact rules as session-integrity.yml.

on:
  push:
    branches:
      - 'claude/**'

jobs:
  merge:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Inspect pushed commit
        id: inspect
        run: |
          set -euo pipefail
          msg=$(git log -1 --pretty=%s ${{ github.sha }})
          echo "Commit message: $msg"

          # Trigger prompts use:
          #   docs/triggers/weekday-session.md → "chore: weekday session {date}"
          #   docs/triggers/weekend-session.md → "chore: weekend crypto session {date}"
          # Accept either, plus the legacy "daily session" variant.
          if ! echo "$msg" | grep -Eq '^chore: (weekday|weekend|daily)( crypto)? session '; then
            echo "Not a daily-session commit — nothing to do."
            echo "is_session=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "is_session=true" >> "$GITHUB_OUTPUT"

      - name: Verify session-integrity rules
        if: steps.inspect.outputs.is_session == 'true'
        run: |
          set -euo pipefail
          files=$(git diff-tree --no-commit-id --name-only -r ${{ github.sha }})
          echo "--- changed files ---"
          echo "$files"
          echo "---------------------"

          touches_artifacts=0
          touches_journals=0
          touches_baselines=0

          if echo "$files" | grep -Eq '^data/(posts|blog|output)/'; then touches_artifacts=1; fi
          if echo "$files" | grep -Eq '^data/agent_memory/.+\.md$'; then touches_journals=1; fi
          if echo "$files" | grep -Eq '^data/baselines/'; then touches_baselines=1; fi

          if [[ $touches_artifacts -eq 0 ]]; then
            echo "::error::Session commit on sandbox branch publishes no artifacts — refusing to merge."
            exit 1
          fi

          fail=0
          if [[ $touches_journals -eq 0 ]]; then
            echo "::error::Session commit missing data/agent_memory/*.md (Step 9 skipped) — refusing to merge."
            fail=1
          fi
          if [[ $touches_baselines -eq 0 ]]; then
            echo "::error::Session commit missing data/baselines/ (Step 9a skipped) — refusing to merge."
            fail=1
          fi
          [[ $fail -eq 0 ]]

      - name: Merge sandbox session into main
        if: steps.inspect.outputs.is_session == 'true'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          session_msg=$(git log -1 --pretty=%s ${{ github.sha }})
          branch_name="${GITHUB_REF#refs/heads/}"

          git fetch origin main
          git checkout main
          git reset --hard origin/main

          git merge --no-ff ${{ github.sha }} \
            -m "Merge sandbox session $branch_name: $session_msg" \
            -m "Auto-merged by .github/workflows/auto-merge-session.yml — main push from cloud sandbox blocked by harness, session commit landed on sandbox branch and this workflow took it the rest of the way."

          git push origin main

      - name: Delete merged sandbox branch
        if: steps.inspect.outputs.is_session == 'true'
        continue-on-error: true
        run: |
          branch_name="${GITHUB_REF#refs/heads/}"
          git push origin --delete "$branch_name"
